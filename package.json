{
  "name": "aegir",
  "private": true,
  "scripts": {
    "build": "npm --prefix web run build && go generate ./internal/frontend && go build -o bin/aegir ./cmd/aegir",
    "dev:api": "go run ./cmd/aegir serve",
    "dev:web": "npm --prefix web run dev",
    "test": "go test ./... && npm --prefix web test && npm --prefix web run typecheck && npm --prefix web run build"
  }
}
